services:
  node-red:
    image: nodered/node-red:3.1.9
    container_name: node-red
    restart: unless-stopped
    ports:
      - "1880:1880"
    volumes:
      - ./data:/data
    environment:
      - TZ=America/New_York
    networks:
      - traefik

networks:
  traefik:
    external: true
