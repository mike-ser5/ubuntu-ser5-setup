http:
  routers:
    immich:
      rule: "Host(`immich.cobblestone`)"
      entryPoints:
        - web
      service: immich

  services:
    immich:
      loadBalancer:
        servers:
          - url: "http://immich-server:2283"
